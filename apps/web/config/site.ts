export type SiteConfig = typeof siteConfig;

export const siteConfig = {
    name: 'Legobase',
    description:
        'A Firebase/Supabase alternative combined with a headless CMS, offering a no-code database solution with a user-friendly GUI. It allows users to create and manage custom tables with flexible field types, automatic API generation, webhook integration, and publicly accessible dashboards, inspired by Notion’s inline databases.',
};
